/**
 * Where opened .eote files are kept, relative to the app root.
 *
 * An export of an opened project writes back into the file it came from,
 * so the bytes have to stay on this machine. Gitignored; never uploaded.
 */
export const IMPORT_DIR = ".imports";
